# Alpha AI 交易系统运行状态报告

## 1. 报告概览

- 报告时间: `2026-03-11 20:45:20 CST`
- 报告对象: `Alpha AI 交易系统`
- 本地前端地址: `http://127.0.0.1:3000/`
- 本地后端地址: `http://127.0.0.1:8000/`
- API 文档地址: `http://127.0.0.1:8000/docs`

本报告基于本机运行态接口核验生成，不是静态代码推断。

## 2. 总体运行状态

当前系统处于在线状态，前后端均可访问。

### 后端实时状态

- 系统状态: `running`
- 运行时长: `439.4` 小时
- 健康分: `85`
- 活跃策略数: `16`
- 今日信号数: `3`
- OODA 循环次数: `1247`
- 决策准确率: `0.68`

### 学习状态

- 今日学习轮次: `3`
- 因子调整次数: `18`
- 在线更新次数: `12`
- 运行中的实验数: `2`
- 今日新部署因子: `2`
- 决策准确率: `0.68`

## 3. 当前交易快照

### 当日信号

当前接口返回 `3` 条信号，时间均为 `2026-03-11 16:47:51` 左右，全部来自 `overnight` 策略。

| 排名 | 代码 | 名称 | 评分 | 买入价 | 止损价 | 目标价 | 风险收益比 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `601860` | 紫金银行 | `0.9778` | `2.76` | `2.69` | `2.99` | `2.5` |
| 2 | `002091` | 江苏国泰 | `0.9717` | `9.08` | `8.84` | `9.84` | `2.5` |
| 3 | `600787` | 中储股份 | `0.9675` | `5.78` | `5.63` | `6.26` | `2.5` |

### 当前持仓

当前持仓共 `4` 只，总市值约 `40234`，总浮盈约 `+754`。

| 代码 | 名称 | 数量 | 成本价 | 现价 | 市值 | 浮盈亏 | 浮盈亏比 | 策略 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `603306` | 江淮汽车 | `1000` | `10.00` | `10.52` | `10520` | `+520` | `+5.20%` | `auction` |
| `603588` | 高能环境 | `800` | `12.50` | `12.98` | `10384` | `+384` | `+3.84%` | `auction` |
| `600519` | 贵州茅台 | `6` | `1580.00` | `1585.00` | `9510` | `+30` | `+0.32%` | `trend` |
| `000001` | 平安银行 | `1000` | `10.00` | `9.82` | `9820` | `-180` | `-1.80%` | `dip_buy` |

## 4. 接口核验结论

本次已核验的关键接口如下，均返回正常:

- `GET /api/system`
- `GET /api/learning`
- `GET /api/signals?days=1`
- `GET /api/positions`
- `GET /api/strategies`

结论:

- 前端页面可访问
- 后端接口正常响应
- 当日信号、持仓、学习数据均能读取
- 今日信号统计已与实际信号数量对齐

## 5. 本次修复记录

### 已修复问题

此前系统状态接口 `GET /api/system` 返回的 `today_signals` 为 `0`，但信号接口实际返回了 `3` 条 `2026-03-11` 的实时信号。

### 根因

- 系统状态统计使用的是 `rec_date`
- 实时信号数据实际字段是 `timestamp`
- 导致“今日信号数”统计失真

### 修复方式

已在后端增加统一的信号日期提取逻辑，兼容:

- `timestamp`
- `rec_date`

修复后已重启后端，并验证:

- `GET /api/system` -> `today_signals = 3`
- `GET /api/signals?days=1` -> 返回 `3` 条信号

## 6. 当前待关注项

### 1. 策略表现统计异常

`GET /api/strategies` 当前能返回策略列表，但近 7 天的 `signal_count`、`win_rate`、`avg_return` 基本都为 `0`。

这通常意味着两种可能:

- `scorecard` 数据尚未同步到接口依赖的数据源
- 策略统计字段和运行期记录口径未完全对齐

这项不影响系统是否在线，但会影响管理端和移动端对策略表现的判断。

### 2. 持仓收益率展示精度

持仓接口中的 `profit_loss_pct` 目前保留了较长浮点尾数，例如:

- `5.200000000000005`
- `3.839999999999999`

这是展示层问题，不影响核心计算，但建议在接口层或前端统一保留两位小数。

## 7. 结论

截至 `2026-03-11 20:45:20 CST`，Alpha AI 交易系统整体运行正常，前后端可访问，核心接口可用，实时信号与持仓数据可读，学习模块在线。

本次已修复一个关键统计问题:

- 系统状态页中的今日信号数统计错误

当前更值得继续处理的方向有两个:

1. 校验 `策略表现` 接口为何近 7 天统计全为 `0`
2. 统一收益率和百分比字段的展示精度
